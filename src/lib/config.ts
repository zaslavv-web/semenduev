/**
 * Адрес обработчика форм на вашем хостинге.
 *
 * - При локальной разработке/превью можно оставить относительный путь "/send.php"
 *   — формы будут отправляться на тот же домен, где открыт сайт.
 * - На бою (после переезда на российский хостинг) убедитесь, что send.php
 *   лежит в корне сайта (https://semenduev.pro/send.php) — тогда этот путь
 *   будет работать без изменений.
 * - Если обработчик будет на другом домене/поддомене, укажите полный URL,
 *   например: "https://api.semenduev.pro/send.php"
 */
export const FORM_ENDPOINT = "send.php";

/**
 * Источник заявки — передаётся в теле запроса, чтобы в письме было видно,
 * с какой формы пришла заявка.
 */
export type FormSource = "contact" | "checklist" | "dialog";
